<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hundsun.demo.springcloud.security.mapper.RoleMapper">
    <select id="getRoleByUserId" resultType="com.hundsun.demo.springcloud.security.pojo.Role">
        select *
        from security_role
        <where>
            <if test="userId != null">
                id in ( select role_id from security_user_role where user_id = #{userId} )
            </if>
        </where>
    </select>
</mapper>