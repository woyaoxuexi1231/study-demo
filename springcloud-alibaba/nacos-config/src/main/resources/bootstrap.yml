spring:
  application:
    name: nacos-config
  cloud:
    nacos:
      config:
        username: nacos
        password: nacos
        server-addr: 127.0.0.1:8848
        namespace: public

server:
  port: 13001

logging:
  level:
    root: debug