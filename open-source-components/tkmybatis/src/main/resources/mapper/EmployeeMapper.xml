<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.hulei.springboot.tkmybatis.mapper.EmployeeMapper">
    <!-- <cache/> -->
    <insert id="saveOne" parameterType="org.hulei.common.mapper.entity.pojo.EmployeeDO">
        insert into employees(employee_number, last_name, first_name, extension, email, office_code, reports_to,
                              job_title)
        values (#{employeeNumber}, #{lastName}, #{firstName}, #{extension}, #{email}, #{officeCode}, #{reportsTo},
                #{jobTitle})
    </insert>
    <select id="selectAllData" resultType="org.hulei.common.mapper.entity.pojo.EmployeeDO">
        select *
        from employees
    </select>

    <select id="selectLastNameById" resultType="java.lang.String">
        select t.last_name
        from employees t
        where t.employee_number = #{id}
    </select>
</mapper>
