<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hundsun.demo.springboot.mybatisplus.mapper.UserMapper">
    <select id="pageList" resultType="com.hundsun.demo.commom.core.model.User">
        SELECT id, name, age, email
        FROM {h- schema} user
    </select>
    <!--
     prefix - 拼接前缀
     suffix - 拼接后缀
     prefixOverrides - 去除 sql 语句前面的关键字或者字符, 该关键字或者字符由 prefixOverrides 属性指定, 假设该属性指定为"AND" 当sql语句的开头为 "AND", trim标签将会去除该 "AND"
     suffixOverrides - 去除 sql 语句后面的关键字或者字符, 该关键字或者字符由 suffixOverrides 属性指定
     -->
    <update id="updateBatchByCaseWhen">
        update user
        <trim prefix="set" suffixOverrides=",">
            <trim prefix="name =case" suffix="end,">
                <foreach collection="list" item="item" index="index">
                    <if test="item.name != null">
                        when id = #{item.id} then #{item.name}
                    </if>
                </foreach>
            </trim>
            <trim prefix="age =case" suffix="end,">
                <foreach collection="list" item="item" index="index">
                    <if test="item.age != null">
                        when id = #{item.id} then #{item.age}
                    </if>
                </foreach>
            </trim>
        </trim>
        where id in
        <foreach collection="list" index="index" item="item" separator="," open="(" close=")">
            #{item.id,jdbcType=BIGINT}
        </foreach>
    </update>

    <select id="selectAll">
    </select>

    <update id="updateBatch">
        <foreach collection="list" index="index" item="item" separator=";" open="" close="">
            update user
            set name = #{item.name}
            where id = #{item.id}
        </foreach>
    </update>
</mapper>